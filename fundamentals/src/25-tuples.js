"use strict";
const courseRecord = ["Typescript Typescript-Bootcamp", "learn fundamentals", 100];
function creaateCourse(title, subtitle) {
    console.log(` Creating a course with Title: ${title}, with Subtitle: ${subtitle}`);
    return [title, subtitle, 100];
}
